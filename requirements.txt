streamlit>=1.32.0
plotly>=5.24.0  # Interactive/clickable charts (Knowledge Base category breakdown)
oracledb>=2.0.0
pandas>=2.0.0
openpyxl>=3.1.0
scikit-learn>=1.3.0
rapidfuzz>=3.6.0
joblib>=1.3.0
python-docx>=1.1.0
pypdf>=5.0.0  # PDF text extraction for PSLD - Parts resolution KB attachments
mammoth>=1.6.0  # DOCX -> HTML conversion for the in-app native document viewer (PSLD - Parts KB)
xlrd>=2.0.1  # legacy .xls support for the docs/ archive import + native viewer (pandas needs this for old Excel format)
pywin32>=306  # Optional: legacy .doc text extraction via Word COM automation (Windows + MS Word only)
filelock>=3.13.0

# AI/LLM dependencies for Text-to-SQL
openai>=1.12.0
anthropic>=0.18.0
google-genai>=0.2.0
requests>=2.31.0
github-copilot-sdk>=0.1.0
msal>=1.28.0  # Azure AD / Entra ID auth (ServiceNow SSO integration POC)
deep-translator>=1.11.0  # Free, no-API-key translation fallback for KB bilingual text (Internal AI / Pendências)
playwright>=1.61.0  # Browser automation for ServiceNow "captured session" experiment (Lab Test tab, option 5) — run `playwright install chromium` after installing
streamlit-cookies-controller>=0.0.4  # Browser-cookie-based login session persistence (replaces the old, insecure URL-query-param token — see auth/session_store.py)
